server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3308/point-db # DB ??
    username: root # DB ??
    password: password # DB ????
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true